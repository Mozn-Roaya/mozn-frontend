/**
 * Alert Inbox domain types. The contract types live in the shared types/ layer
 * (mirroring the Go internal/model); re-exported here for the feature's own
 * components.
 */
export type * from "@/types/alert-inbox";
